import React from "react";
import { Card } from "antd";

const EmployeeMiniCard = ({ id, name, onClick }) => {
  return (
    <Card hoverable onClick={onClick}>
     
    </Card>
  );
};

export default EmployeeMiniCard;
